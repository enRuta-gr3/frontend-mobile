/*import { View, Button, Text } from "react-native";

type Props = {
    onPay: () => void;
    onCancel: () => void;
};

export default function Payment({ onPay, onCancel }: Props) {
  return (
    <View style={{ gap: 20}}>
      <Text style={{ fontSize: 20, textAlign: "center"}}>Pagar con Mercado Pago</Text>
      <Button title="Pagar" onPress={onPay} />
      <Button title="Cancelar" onPress={onCancel} />
    </View>
  );
}
*/